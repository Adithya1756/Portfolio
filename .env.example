# Choose ONE email delivery method for the contact form.

# Option A — Resend (recommended, simplest for Vercel)
RESEND_API_KEY=
CONTACT_TO_EMAIL=adithya1756@gmail.com
CONTACT_FROM_EMAIL=onboarding@resend.dev

# Option B — SMTP via Nodemailer (e.g. Gmail App Password, or any SMTP host)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
